__pycache__/
*.py[cod]
.venv/
.DS_Store
Thumbs.db

# Secrets: all runtime configuration comes from .env, which is never committed.
# .env.example ships a placeholder instead. config/app.env was deleted in ea1d99b
# because it held a real credential; this entry stops it coming back.
.env
.env.*
!.env.example
config/app.env

# Local-only artefacts
evidence/local/
.assessment/
backups/
*.dump
